[package]
name = "useskiasharp_native"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
path = "lib.rs"

[target.'cfg(any(not(target_os = "ios"), target_abi = "macabi"))'.dependencies]
libloading = "0.8"
once_cell = "1.19"
